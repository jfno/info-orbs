#ifndef CAROUSEL_CONFIG_H
#define CAROUSEL_CONFIG_H

#include "Carousel.h"

// Builds the ordered tile ribbon and its data sources. This is the authoritative
// place to reorder / enable / disable tiles -- edit the add() sequence below.
void buildCarousel(Carousel &carousel);

#endif // CAROUSEL_CONFIG_H
